<%
  var keywords = '';

  var terms = [];

  if (page.categories) {
    
  }


%>

  <meta name="description"
        content="<%= description || config.description %>">

  <meta name="author"
        content="<%= page.author || config.author %>">
